@page "/reports-mesure"


<h3>Таблица адресов по дате замера</h3>

<MenuForSearchingAddresses OnAddressesLoad="LoadAddresses"></MenuForSearchingAddresses>
<br />
<ViewTableWithAddresses addresses="addresses"></ViewTableWithAddresses>

@code {
    private IEnumerable<Address> addresses;

    private void LoadAddresses(IEnumerable<Address> addresses)
    {
        this.addresses = addresses;
    }
}
